# 新项目初始化检查清单

复制模板后，从第一项往下过。带 ★ 的是必做项。

## 一、立项

- [ ] ★ 文件夹改名为项目名（ASCII 小写 + 连字符，如 `a10-cleaner`）
- [ ] ★ 把 `README.md` 里的 `__PROJECT__` 换成项目名
- [ ] ★ 在**根目录** `git init` + 首次提交（先把空骨架提一次，之后每次提交都有干净的基线）
- [ ] 在 `06_management/项目计划/` 建 `项目计划_V1.0_YYYYMMDD.md`：排期、里程碑、打样轮次
- [ ] 在 `06_management/风险登记/` 建 `风险登记表_V1.0_YYYYMMDD.md`

## 二、输入资料

- [ ] ★ 客户规格书、图纸、外采要求 → `00_input/客户需求/`
- [ ] 厂商 datasheet、参考方案、竞品拆解 → `00_input/参考设计/`
- [ ] 历次沟通的邮件/纪要/聊天导出 → `00_input/沟通记录/`（命名 `YYYYMMDD_对方_主题`）
- [ ] 确认 `00_input/` 全部只读：只往里丢原件，不做加工

## 三、设计文档（`01_docs/`，每个文档一个文件夹）

- [ ] ★ `需求规格说明书/` 写 V1.0，填 `版本索引.md`
- [ ] `系统方案设计/` 整体框图、控制策略（六步换相 / FOC）、关键器件选型
- [ ] `接口定义文档/` 对外接口：UART/CAN 报文、引脚分配表、连接器定义
- [ ] ★ `电机参数与标定/` 填写电机本体参数：R / L / Ke / 极对数 / 额定电流，以及电流环、速度环 PID 初值

## 四、硬件

- [ ] `02_hardware/Schematic/`、`PCB/`、`BOM/` 首版（命名 `Vx.x_YYYYMMDD`）
- [ ] `02_hardware/结构_3D/` 外壳、线束、开关、装配约束
- [ ] `02_hardware/打样记录/V1.0_YYYYMMDD/` 建目录，记录板厂、Gerber、回样实测

## 五、固件

- [ ] ★ 在 `03_software/` 下**另外** `git init` 一个仓库管固件（固件独立管控，不并进根仓库；理由见 `README.md` 第一节末）
- [ ] ★ 给固件仓库单独 `git remote add origin <地址>`（可设为私有，不必跟着模板仓库公开）
- [ ] `03_software/01_reference/` 放厂商 SDK / demo（只读，不要改）
- [ ] ★ 在 `03_software/03_debug/` 里用 Keil **新建**工程（模板不含代码骨架，`.uvprojx` 由 Keil 生成）
- [ ] 建 `03_software/02_unit_test/`：能跑在 PC 上的纯算法单测（PID、滤波、CRC）

## 六、收尾

- [ ] 在 `06_management/开发日志/` 建当月的 `YYYY-MM.md`，写下第一条记录
- [ ] 检查根目录 `.gitignore`（根仓库）与 `03_software/.gitignore`（固件仓库）是否需要补充本项目特有的中间产物
- [ ] 删掉仍为空的目录里的 `.gitkeep`（或保留，无妨）

---

> 每次做完一件有追溯价值的事（客户确认了某件事、改了某个参数、打样回来、发现一个问题），
> 立刻在 `06_management/开发日志/` 当月文件里补一行。这是整个模板里最容易漏、也最值钱的动作。
